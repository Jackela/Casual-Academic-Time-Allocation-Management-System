/**
 * Main Configuration Entry Point
 *
 * Surface unified configuration utilities for application code.
 */

export { getConfig, isFeatureEnabled, getApiEndpoint, getEndpoint } from './config/unified-config';
export { initializeConfiguration, getConfigurationSummary } from './config/index';
